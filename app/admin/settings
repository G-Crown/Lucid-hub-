cat > $B/app/admin/settings/page.tsx << 'EOF'
import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Settings' }
export default function AdminSettingsPage() {
  return (
    <div className="p-8 max-w-3xl">
      <h1 className="font-serif text-3xl font-bold text-white mb-1">Settings</h1>
      <p className="text-white/40 text-sm mb-8">Manage site configuration and admin account.</p>
      <div className="space-y-6">
        {[
          { title:'Organisation Info', fields:[{ label:'Organisation Name', val:'Lucid Hub' },{ label:'Contact Email', val:'lucidhub.info@gmail.com' },{ label:'Location', val:'Lagos, Nigeria' }] },
          { title:'Admin Account', fields:[{ label:'Full Name', val:'G.Crown (Olugbenga Stephen Oke)' },{ label:'Email', val:'lucidhub.info@gmail.com' },{ label:'Role', val:'Admin' }] },
        ].map(section => (
          <div key={section.title} className="bg-white/[0.04] border border-white/10 rounded-2xl p-6">
            <h2 className="font-semibold text-white mb-5">{section.title}</h2>
            <div className="space-y-4">
              {section.fields.map(f => (
                <div key={f.label}>
                  <label className="block text-xs font-bold text-white/40 uppercase tracking-wider mb-1.5">{f.label}</label>
                  <input defaultValue={f.val} className="w-full bg-white/[0.06] border border-white/15 rounded-lg px-4 py-3 text-white text-sm outline-none focus:border-[#1A1AFF] transition-colors"/>
                </div>
              ))}
            </div>
            <button className="mt-5 bg-[#1A1AFF] text-white px-5 py-2.5 rounded-lg text-sm font-semibold hover:bg-[#3B3BFF] transition-colors">
              Save Changes
            </button>
          </div>
        ))}
      </div>
    </div>
  )
}

